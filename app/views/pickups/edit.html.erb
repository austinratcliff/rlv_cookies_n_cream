<%= form_with model: @pickup, url: order_pickup_path(@order), method: :put do |form| %>
  <%= form.label :pickup_at, 'Pickup date and time:' %><br>
  <%= form.datetime_field :pickup_at %><br>
  <%= form.submit 'Save' %>
<% end %>
